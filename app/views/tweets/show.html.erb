<p id="notice"><%= notice %></p>
<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <div class="tweet-text">
      <h3 class="left"><%= @tweet.content %></h3>
      <p><%= image_tag @tweet.image_url %></p>
      <p class="right">
        <% if current_user.id == @tweet.user_id %>
          <%= link_to "編集", edit_tweet_path(@tweet.id), class: 'btn btn-default btn-sm btn-info' %>
          <%= link_to "削除", tweet_path(@tweet.id), method: :delete, data: { confirm: '本当に削除していいですか？'}, class: 'btn btn-default btn-sm btn btn-warning' %>
        <% end %>
        <strong>作成時間:</strong>
        <%= @tweet.created_at.strftime("%y/%m/%d %p %l:%M") %>
      </p>
    </div>
    <p>コメント一覧</p>
    <div id="comments_area">
      <%= render partial: 'comments/index', locals: { comments: @comments, tweet: @tweet } %>
    </div>
    <%= render partial: 'comments/form', locals: { comment: @comment, tweet: @tweet } %>
    <%= link_to '戻る', tweets_path %>
  </div>
</div>